
"""
    PREKIT API

    PREKIT API Documentation

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations

import json
import pprint
from typing import Any

from pydantic import BaseModel, ConfigDict, ValidationError, field_validator
from typing_extensions import Self

from prekit_edge_node_api.models.gauge_panel import GaugePanel
from prekit_edge_node_api.models.stat_panel import StatPanel
from prekit_edge_node_api.models.text_panel import TextPanel
from prekit_edge_node_api.models.timeseries_panel import TimeseriesPanel

PANEL_ONE_OF_SCHEMAS = ["GaugePanel", "StatPanel", "TextPanel", "TimeseriesPanel"]

class Panel(BaseModel):
    """
    Panel
    """
    # data type: TextPanel
    oneof_schema_1_validator: TextPanel | None = None
    # data type: TimeseriesPanel
    oneof_schema_2_validator: TimeseriesPanel | None = None
    # data type: GaugePanel
    oneof_schema_3_validator: GaugePanel | None = None
    # data type: StatPanel
    oneof_schema_4_validator: StatPanel | None = None
    actual_instance: GaugePanel | StatPanel | TextPanel | TimeseriesPanel | None = None
    one_of_schemas: set[str] = { "GaugePanel", "StatPanel", "TextPanel", "TimeseriesPanel" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = Panel.model_construct()
        error_messages = []
        match = 0
        # validate data type: TextPanel
        if not isinstance(v, TextPanel):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TextPanel`")
        else:
            match += 1
        # validate data type: TimeseriesPanel
        if not isinstance(v, TimeseriesPanel):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TimeseriesPanel`")
        else:
            match += 1
        # validate data type: GaugePanel
        if not isinstance(v, GaugePanel):
            error_messages.append(f"Error! Input type `{type(v)}` is not `GaugePanel`")
        else:
            match += 1
        # validate data type: StatPanel
        if not isinstance(v, StatPanel):
            error_messages.append(f"Error! Input type `{type(v)}` is not `StatPanel`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in Panel with oneOf schemas: GaugePanel, StatPanel, TextPanel, TimeseriesPanel. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in Panel with oneOf schemas: GaugePanel, StatPanel, TextPanel, TimeseriesPanel. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: str | dict[str, Any]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into TextPanel
        try:
            instance.actual_instance = TextPanel.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into TimeseriesPanel
        try:
            instance.actual_instance = TimeseriesPanel.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into GaugePanel
        try:
            instance.actual_instance = GaugePanel.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into StatPanel
        try:
            instance.actual_instance = StatPanel.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into Panel with oneOf schemas: GaugePanel, StatPanel, TextPanel, TimeseriesPanel. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into Panel with oneOf schemas: GaugePanel, StatPanel, TextPanel, TimeseriesPanel. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> dict[str, Any] | GaugePanel | StatPanel | TextPanel | TimeseriesPanel | None:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


